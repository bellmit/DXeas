package com.kingdee.eas.custom.shr;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class RecordBusChangeCollection extends AbstractObjectCollection 
{
    public RecordBusChangeCollection()
    {
        super(RecordBusChangeInfo.class);
    }
    public boolean add(RecordBusChangeInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(RecordBusChangeCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(RecordBusChangeInfo item)
    {
        return removeObject(item);
    }
    public RecordBusChangeInfo get(int index)
    {
        return(RecordBusChangeInfo)getObject(index);
    }
    public RecordBusChangeInfo get(Object key)
    {
        return(RecordBusChangeInfo)getObject(key);
    }
    public void set(int index, RecordBusChangeInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(RecordBusChangeInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(RecordBusChangeInfo item)
    {
        return super.indexOf(item);
    }
}