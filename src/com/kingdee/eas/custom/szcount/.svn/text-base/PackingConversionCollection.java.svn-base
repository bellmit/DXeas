package com.kingdee.eas.custom.szcount;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class PackingConversionCollection extends AbstractObjectCollection 
{
    public PackingConversionCollection()
    {
        super(PackingConversionInfo.class);
    }
    public boolean add(PackingConversionInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(PackingConversionCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(PackingConversionInfo item)
    {
        return removeObject(item);
    }
    public PackingConversionInfo get(int index)
    {
        return(PackingConversionInfo)getObject(index);
    }
    public PackingConversionInfo get(Object key)
    {
        return(PackingConversionInfo)getObject(key);
    }
    public void set(int index, PackingConversionInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(PackingConversionInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(PackingConversionInfo item)
    {
        return super.indexOf(item);
    }
}