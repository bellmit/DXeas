package com.kingdee.eas.custom.szcount;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class FreshDailyEntryCollection extends AbstractObjectCollection 
{
    public FreshDailyEntryCollection()
    {
        super(FreshDailyEntryInfo.class);
    }
    public boolean add(FreshDailyEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(FreshDailyEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(FreshDailyEntryInfo item)
    {
        return removeObject(item);
    }
    public FreshDailyEntryInfo get(int index)
    {
        return(FreshDailyEntryInfo)getObject(index);
    }
    public FreshDailyEntryInfo get(Object key)
    {
        return(FreshDailyEntryInfo)getObject(key);
    }
    public void set(int index, FreshDailyEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(FreshDailyEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(FreshDailyEntryInfo item)
    {
        return super.indexOf(item);
    }
}