package com.kingdee.eas.custom.salaryvoucher;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class SCalSchemeCollection extends AbstractObjectCollection 
{
    public SCalSchemeCollection()
    {
        super(SCalSchemeInfo.class);
    }
    public boolean add(SCalSchemeInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(SCalSchemeCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(SCalSchemeInfo item)
    {
        return removeObject(item);
    }
    public SCalSchemeInfo get(int index)
    {
        return(SCalSchemeInfo)getObject(index);
    }
    public SCalSchemeInfo get(Object key)
    {
        return(SCalSchemeInfo)getObject(key);
    }
    public void set(int index, SCalSchemeInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(SCalSchemeInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(SCalSchemeInfo item)
    {
        return super.indexOf(item);
    }
}