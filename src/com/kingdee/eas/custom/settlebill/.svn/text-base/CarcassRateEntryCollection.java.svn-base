package com.kingdee.eas.custom.settlebill;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class CarcassRateEntryCollection extends AbstractObjectCollection 
{
    public CarcassRateEntryCollection()
    {
        super(CarcassRateEntryInfo.class);
    }
    public boolean add(CarcassRateEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(CarcassRateEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(CarcassRateEntryInfo item)
    {
        return removeObject(item);
    }
    public CarcassRateEntryInfo get(int index)
    {
        return(CarcassRateEntryInfo)getObject(index);
    }
    public CarcassRateEntryInfo get(Object key)
    {
        return(CarcassRateEntryInfo)getObject(key);
    }
    public void set(int index, CarcassRateEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(CarcassRateEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(CarcassRateEntryInfo item)
    {
        return super.indexOf(item);
    }
}