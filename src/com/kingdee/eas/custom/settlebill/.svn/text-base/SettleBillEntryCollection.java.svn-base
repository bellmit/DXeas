package com.kingdee.eas.custom.settlebill;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class SettleBillEntryCollection extends AbstractObjectCollection 
{
    public SettleBillEntryCollection()
    {
        super(SettleBillEntryInfo.class);
    }
    public boolean add(SettleBillEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(SettleBillEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(SettleBillEntryInfo item)
    {
        return removeObject(item);
    }
    public SettleBillEntryInfo get(int index)
    {
        return(SettleBillEntryInfo)getObject(index);
    }
    public SettleBillEntryInfo get(Object key)
    {
        return(SettleBillEntryInfo)getObject(key);
    }
    public void set(int index, SettleBillEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(SettleBillEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(SettleBillEntryInfo item)
    {
        return super.indexOf(item);
    }
}