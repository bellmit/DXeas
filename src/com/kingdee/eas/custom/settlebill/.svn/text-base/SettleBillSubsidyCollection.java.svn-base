package com.kingdee.eas.custom.settlebill;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class SettleBillSubsidyCollection extends AbstractObjectCollection 
{
    public SettleBillSubsidyCollection()
    {
        super(SettleBillSubsidyInfo.class);
    }
    public boolean add(SettleBillSubsidyInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(SettleBillSubsidyCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(SettleBillSubsidyInfo item)
    {
        return removeObject(item);
    }
    public SettleBillSubsidyInfo get(int index)
    {
        return(SettleBillSubsidyInfo)getObject(index);
    }
    public SettleBillSubsidyInfo get(Object key)
    {
        return(SettleBillSubsidyInfo)getObject(key);
    }
    public void set(int index, SettleBillSubsidyInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(SettleBillSubsidyInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(SettleBillSubsidyInfo item)
    {
        return super.indexOf(item);
    }
}