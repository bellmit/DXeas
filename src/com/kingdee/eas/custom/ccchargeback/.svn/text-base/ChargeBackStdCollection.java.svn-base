package com.kingdee.eas.custom.ccchargeback;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class ChargeBackStdCollection extends AbstractObjectCollection 
{
    public ChargeBackStdCollection()
    {
        super(ChargeBackStdInfo.class);
    }
    public boolean add(ChargeBackStdInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(ChargeBackStdCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(ChargeBackStdInfo item)
    {
        return removeObject(item);
    }
    public ChargeBackStdInfo get(int index)
    {
        return(ChargeBackStdInfo)getObject(index);
    }
    public ChargeBackStdInfo get(Object key)
    {
        return(ChargeBackStdInfo)getObject(key);
    }
    public void set(int index, ChargeBackStdInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(ChargeBackStdInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(ChargeBackStdInfo item)
    {
        return super.indexOf(item);
    }
}