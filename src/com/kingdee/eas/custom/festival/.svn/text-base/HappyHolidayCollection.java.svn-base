package com.kingdee.eas.custom.festival;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class HappyHolidayCollection extends AbstractObjectCollection 
{
    public HappyHolidayCollection()
    {
        super(HappyHolidayInfo.class);
    }
    public boolean add(HappyHolidayInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(HappyHolidayCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(HappyHolidayInfo item)
    {
        return removeObject(item);
    }
    public HappyHolidayInfo get(int index)
    {
        return(HappyHolidayInfo)getObject(index);
    }
    public HappyHolidayInfo get(Object key)
    {
        return(HappyHolidayInfo)getObject(key);
    }
    public void set(int index, HappyHolidayInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(HappyHolidayInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(HappyHolidayInfo item)
    {
        return super.indexOf(item);
    }
}