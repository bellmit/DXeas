package com.kingdee.eas.custom.festival;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class FactoryCalendarAssEntryCollection extends AbstractObjectCollection 
{
    public FactoryCalendarAssEntryCollection()
    {
        super(FactoryCalendarAssEntryInfo.class);
    }
    public boolean add(FactoryCalendarAssEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(FactoryCalendarAssEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(FactoryCalendarAssEntryInfo item)
    {
        return removeObject(item);
    }
    public FactoryCalendarAssEntryInfo get(int index)
    {
        return(FactoryCalendarAssEntryInfo)getObject(index);
    }
    public FactoryCalendarAssEntryInfo get(Object key)
    {
        return(FactoryCalendarAssEntryInfo)getObject(key);
    }
    public void set(int index, FactoryCalendarAssEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(FactoryCalendarAssEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(FactoryCalendarAssEntryInfo item)
    {
        return super.indexOf(item);
    }
}