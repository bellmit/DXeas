package com.kingdee.eas.custom.independence;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class PermissionSettingCollection extends AbstractObjectCollection 
{
    public PermissionSettingCollection()
    {
        super(PermissionSettingInfo.class);
    }
    public boolean add(PermissionSettingInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(PermissionSettingCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(PermissionSettingInfo item)
    {
        return removeObject(item);
    }
    public PermissionSettingInfo get(int index)
    {
        return(PermissionSettingInfo)getObject(index);
    }
    public PermissionSettingInfo get(Object key)
    {
        return(PermissionSettingInfo)getObject(key);
    }
    public void set(int index, PermissionSettingInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(PermissionSettingInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(PermissionSettingInfo item)
    {
        return super.indexOf(item);
    }
}