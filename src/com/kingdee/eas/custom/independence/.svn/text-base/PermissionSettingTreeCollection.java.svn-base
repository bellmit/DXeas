package com.kingdee.eas.custom.independence;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class PermissionSettingTreeCollection extends AbstractObjectCollection 
{
    public PermissionSettingTreeCollection()
    {
        super(PermissionSettingTreeInfo.class);
    }
    public boolean add(PermissionSettingTreeInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(PermissionSettingTreeCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(PermissionSettingTreeInfo item)
    {
        return removeObject(item);
    }
    public PermissionSettingTreeInfo get(int index)
    {
        return(PermissionSettingTreeInfo)getObject(index);
    }
    public PermissionSettingTreeInfo get(Object key)
    {
        return(PermissionSettingTreeInfo)getObject(key);
    }
    public void set(int index, PermissionSettingTreeInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(PermissionSettingTreeInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(PermissionSettingTreeInfo item)
    {
        return super.indexOf(item);
    }
}