package com.kingdee.eas.custom.taihe.workshopmanager;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class LoadingBillEntryCollection extends AbstractObjectCollection 
{
    public LoadingBillEntryCollection()
    {
        super(LoadingBillEntryInfo.class);
    }
    public boolean add(LoadingBillEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(LoadingBillEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(LoadingBillEntryInfo item)
    {
        return removeObject(item);
    }
    public LoadingBillEntryInfo get(int index)
    {
        return(LoadingBillEntryInfo)getObject(index);
    }
    public LoadingBillEntryInfo get(Object key)
    {
        return(LoadingBillEntryInfo)getObject(key);
    }
    public void set(int index, LoadingBillEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(LoadingBillEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(LoadingBillEntryInfo item)
    {
        return super.indexOf(item);
    }
}