package com.kingdee.eas.custom.taihe.weight;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class WgtDisRateEntryCollection extends AbstractObjectCollection 
{
    public WgtDisRateEntryCollection()
    {
        super(WgtDisRateEntryInfo.class);
    }
    public boolean add(WgtDisRateEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(WgtDisRateEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(WgtDisRateEntryInfo item)
    {
        return removeObject(item);
    }
    public WgtDisRateEntryInfo get(int index)
    {
        return(WgtDisRateEntryInfo)getObject(index);
    }
    public WgtDisRateEntryInfo get(Object key)
    {
        return(WgtDisRateEntryInfo)getObject(key);
    }
    public void set(int index, WgtDisRateEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(WgtDisRateEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(WgtDisRateEntryInfo item)
    {
        return super.indexOf(item);
    }
}