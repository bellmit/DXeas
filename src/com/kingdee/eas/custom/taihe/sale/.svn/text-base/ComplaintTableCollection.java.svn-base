package com.kingdee.eas.custom.taihe.sale;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class ComplaintTableCollection extends AbstractObjectCollection 
{
    public ComplaintTableCollection()
    {
        super(ComplaintTableInfo.class);
    }
    public boolean add(ComplaintTableInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(ComplaintTableCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(ComplaintTableInfo item)
    {
        return removeObject(item);
    }
    public ComplaintTableInfo get(int index)
    {
        return(ComplaintTableInfo)getObject(index);
    }
    public ComplaintTableInfo get(Object key)
    {
        return(ComplaintTableInfo)getObject(key);
    }
    public void set(int index, ComplaintTableInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(ComplaintTableInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(ComplaintTableInfo item)
    {
        return super.indexOf(item);
    }
}