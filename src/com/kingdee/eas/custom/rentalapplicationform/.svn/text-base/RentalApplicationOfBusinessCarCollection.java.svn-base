package com.kingdee.eas.custom.rentalapplicationform;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class RentalApplicationOfBusinessCarCollection extends AbstractObjectCollection 
{
    public RentalApplicationOfBusinessCarCollection()
    {
        super(RentalApplicationOfBusinessCarInfo.class);
    }
    public boolean add(RentalApplicationOfBusinessCarInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(RentalApplicationOfBusinessCarCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(RentalApplicationOfBusinessCarInfo item)
    {
        return removeObject(item);
    }
    public RentalApplicationOfBusinessCarInfo get(int index)
    {
        return(RentalApplicationOfBusinessCarInfo)getObject(index);
    }
    public RentalApplicationOfBusinessCarInfo get(Object key)
    {
        return(RentalApplicationOfBusinessCarInfo)getObject(key);
    }
    public void set(int index, RentalApplicationOfBusinessCarInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(RentalApplicationOfBusinessCarInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(RentalApplicationOfBusinessCarInfo item)
    {
        return super.indexOf(item);
    }
}