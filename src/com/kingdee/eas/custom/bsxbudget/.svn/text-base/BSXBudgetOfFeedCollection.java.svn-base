package com.kingdee.eas.custom.bsxbudget;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class BSXBudgetOfFeedCollection extends AbstractObjectCollection 
{
    public BSXBudgetOfFeedCollection()
    {
        super(BSXBudgetOfFeedInfo.class);
    }
    public boolean add(BSXBudgetOfFeedInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(BSXBudgetOfFeedCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(BSXBudgetOfFeedInfo item)
    {
        return removeObject(item);
    }
    public BSXBudgetOfFeedInfo get(int index)
    {
        return(BSXBudgetOfFeedInfo)getObject(index);
    }
    public BSXBudgetOfFeedInfo get(Object key)
    {
        return(BSXBudgetOfFeedInfo)getObject(key);
    }
    public void set(int index, BSXBudgetOfFeedInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(BSXBudgetOfFeedInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(BSXBudgetOfFeedInfo item)
    {
        return super.indexOf(item);
    }
}