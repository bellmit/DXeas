package com.kingdee.eas.custom.wages;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class FractionBillCollection extends AbstractObjectCollection 
{
    public FractionBillCollection()
    {
        super(FractionBillInfo.class);
    }
    public boolean add(FractionBillInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(FractionBillCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(FractionBillInfo item)
    {
        return removeObject(item);
    }
    public FractionBillInfo get(int index)
    {
        return(FractionBillInfo)getObject(index);
    }
    public FractionBillInfo get(Object key)
    {
        return(FractionBillInfo)getObject(key);
    }
    public void set(int index, FractionBillInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(FractionBillInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(FractionBillInfo item)
    {
        return super.indexOf(item);
    }
}