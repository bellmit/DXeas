package com.kingdee.eas.custom.wages;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class QuitWagesBillEntryCollection extends AbstractObjectCollection 
{
    public QuitWagesBillEntryCollection()
    {
        super(QuitWagesBillEntryInfo.class);
    }
    public boolean add(QuitWagesBillEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(QuitWagesBillEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(QuitWagesBillEntryInfo item)
    {
        return removeObject(item);
    }
    public QuitWagesBillEntryInfo get(int index)
    {
        return(QuitWagesBillEntryInfo)getObject(index);
    }
    public QuitWagesBillEntryInfo get(Object key)
    {
        return(QuitWagesBillEntryInfo)getObject(key);
    }
    public void set(int index, QuitWagesBillEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(QuitWagesBillEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(QuitWagesBillEntryInfo item)
    {
        return super.indexOf(item);
    }
}