package com.kingdee.eas.custom.wages;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class QuitWagesBillCollection extends AbstractObjectCollection 
{
    public QuitWagesBillCollection()
    {
        super(QuitWagesBillInfo.class);
    }
    public boolean add(QuitWagesBillInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(QuitWagesBillCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(QuitWagesBillInfo item)
    {
        return removeObject(item);
    }
    public QuitWagesBillInfo get(int index)
    {
        return(QuitWagesBillInfo)getObject(index);
    }
    public QuitWagesBillInfo get(Object key)
    {
        return(QuitWagesBillInfo)getObject(key);
    }
    public void set(int index, QuitWagesBillInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(QuitWagesBillInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(QuitWagesBillInfo item)
    {
        return super.indexOf(item);
    }
}