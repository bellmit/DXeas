package com.kingdee.eas.custom.wages;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class EmploySecurityCollection extends AbstractObjectCollection 
{
    public EmploySecurityCollection()
    {
        super(EmploySecurityInfo.class);
    }
    public boolean add(EmploySecurityInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(EmploySecurityCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(EmploySecurityInfo item)
    {
        return removeObject(item);
    }
    public EmploySecurityInfo get(int index)
    {
        return(EmploySecurityInfo)getObject(index);
    }
    public EmploySecurityInfo get(Object key)
    {
        return(EmploySecurityInfo)getObject(key);
    }
    public void set(int index, EmploySecurityInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(EmploySecurityInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(EmploySecurityInfo item)
    {
        return super.indexOf(item);
    }
}