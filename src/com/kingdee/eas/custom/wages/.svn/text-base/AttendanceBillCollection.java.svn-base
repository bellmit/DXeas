package com.kingdee.eas.custom.wages;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class AttendanceBillCollection extends AbstractObjectCollection 
{
    public AttendanceBillCollection()
    {
        super(AttendanceBillInfo.class);
    }
    public boolean add(AttendanceBillInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(AttendanceBillCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(AttendanceBillInfo item)
    {
        return removeObject(item);
    }
    public AttendanceBillInfo get(int index)
    {
        return(AttendanceBillInfo)getObject(index);
    }
    public AttendanceBillInfo get(Object key)
    {
        return(AttendanceBillInfo)getObject(key);
    }
    public void set(int index, AttendanceBillInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(AttendanceBillInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(AttendanceBillInfo item)
    {
        return super.indexOf(item);
    }
}