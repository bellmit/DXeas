package com.kingdee.eas.custom.wages;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class SeniorityBonusEntryCollection extends AbstractObjectCollection 
{
    public SeniorityBonusEntryCollection()
    {
        super(SeniorityBonusEntryInfo.class);
    }
    public boolean add(SeniorityBonusEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(SeniorityBonusEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(SeniorityBonusEntryInfo item)
    {
        return removeObject(item);
    }
    public SeniorityBonusEntryInfo get(int index)
    {
        return(SeniorityBonusEntryInfo)getObject(index);
    }
    public SeniorityBonusEntryInfo get(Object key)
    {
        return(SeniorityBonusEntryInfo)getObject(key);
    }
    public void set(int index, SeniorityBonusEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(SeniorityBonusEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(SeniorityBonusEntryInfo item)
    {
        return super.indexOf(item);
    }
}