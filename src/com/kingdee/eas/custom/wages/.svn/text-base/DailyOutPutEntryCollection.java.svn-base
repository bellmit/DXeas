package com.kingdee.eas.custom.wages;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class DailyOutPutEntryCollection extends AbstractObjectCollection 
{
    public DailyOutPutEntryCollection()
    {
        super(DailyOutPutEntryInfo.class);
    }
    public boolean add(DailyOutPutEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(DailyOutPutEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(DailyOutPutEntryInfo item)
    {
        return removeObject(item);
    }
    public DailyOutPutEntryInfo get(int index)
    {
        return(DailyOutPutEntryInfo)getObject(index);
    }
    public DailyOutPutEntryInfo get(Object key)
    {
        return(DailyOutPutEntryInfo)getObject(key);
    }
    public void set(int index, DailyOutPutEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(DailyOutPutEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(DailyOutPutEntryInfo item)
    {
        return super.indexOf(item);
    }
}