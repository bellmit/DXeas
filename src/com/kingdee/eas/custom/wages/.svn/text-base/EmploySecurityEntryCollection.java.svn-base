package com.kingdee.eas.custom.wages;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class EmploySecurityEntryCollection extends AbstractObjectCollection 
{
    public EmploySecurityEntryCollection()
    {
        super(EmploySecurityEntryInfo.class);
    }
    public boolean add(EmploySecurityEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(EmploySecurityEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(EmploySecurityEntryInfo item)
    {
        return removeObject(item);
    }
    public EmploySecurityEntryInfo get(int index)
    {
        return(EmploySecurityEntryInfo)getObject(index);
    }
    public EmploySecurityEntryInfo get(Object key)
    {
        return(EmploySecurityEntryInfo)getObject(key);
    }
    public void set(int index, EmploySecurityEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(EmploySecurityEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(EmploySecurityEntryInfo item)
    {
        return super.indexOf(item);
    }
}