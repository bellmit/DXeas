package com.kingdee.eas.custom.wages.farm;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class MaintenTeamCollection extends AbstractObjectCollection 
{
    public MaintenTeamCollection()
    {
        super(MaintenTeamInfo.class);
    }
    public boolean add(MaintenTeamInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(MaintenTeamCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(MaintenTeamInfo item)
    {
        return removeObject(item);
    }
    public MaintenTeamInfo get(int index)
    {
        return(MaintenTeamInfo)getObject(index);
    }
    public MaintenTeamInfo get(Object key)
    {
        return(MaintenTeamInfo)getObject(key);
    }
    public void set(int index, MaintenTeamInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(MaintenTeamInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(MaintenTeamInfo item)
    {
        return super.indexOf(item);
    }
}