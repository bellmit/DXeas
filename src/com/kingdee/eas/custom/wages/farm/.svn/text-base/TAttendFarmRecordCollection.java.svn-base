package com.kingdee.eas.custom.wages.farm;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class TAttendFarmRecordCollection extends AbstractObjectCollection 
{
    public TAttendFarmRecordCollection()
    {
        super(TAttendFarmRecordInfo.class);
    }
    public boolean add(TAttendFarmRecordInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(TAttendFarmRecordCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(TAttendFarmRecordInfo item)
    {
        return removeObject(item);
    }
    public TAttendFarmRecordInfo get(int index)
    {
        return(TAttendFarmRecordInfo)getObject(index);
    }
    public TAttendFarmRecordInfo get(Object key)
    {
        return(TAttendFarmRecordInfo)getObject(key);
    }
    public void set(int index, TAttendFarmRecordInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(TAttendFarmRecordInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(TAttendFarmRecordInfo item)
    {
        return super.indexOf(item);
    }
}