package com.kingdee.eas.custom.wages.farm;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class MaintenTeamEntryCollection extends AbstractObjectCollection 
{
    public MaintenTeamEntryCollection()
    {
        super(MaintenTeamEntryInfo.class);
    }
    public boolean add(MaintenTeamEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(MaintenTeamEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(MaintenTeamEntryInfo item)
    {
        return removeObject(item);
    }
    public MaintenTeamEntryInfo get(int index)
    {
        return(MaintenTeamEntryInfo)getObject(index);
    }
    public MaintenTeamEntryInfo get(Object key)
    {
        return(MaintenTeamEntryInfo)getObject(key);
    }
    public void set(int index, MaintenTeamEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(MaintenTeamEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(MaintenTeamEntryInfo item)
    {
        return super.indexOf(item);
    }
}