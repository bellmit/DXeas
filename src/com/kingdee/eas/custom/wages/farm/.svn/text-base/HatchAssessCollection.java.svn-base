package com.kingdee.eas.custom.wages.farm;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class HatchAssessCollection extends AbstractObjectCollection 
{
    public HatchAssessCollection()
    {
        super(HatchAssessInfo.class);
    }
    public boolean add(HatchAssessInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(HatchAssessCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(HatchAssessInfo item)
    {
        return removeObject(item);
    }
    public HatchAssessInfo get(int index)
    {
        return(HatchAssessInfo)getObject(index);
    }
    public HatchAssessInfo get(Object key)
    {
        return(HatchAssessInfo)getObject(key);
    }
    public void set(int index, HatchAssessInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(HatchAssessInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(HatchAssessInfo item)
    {
        return super.indexOf(item);
    }
}