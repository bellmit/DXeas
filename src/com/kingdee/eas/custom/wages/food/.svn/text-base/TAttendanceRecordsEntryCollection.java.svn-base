package com.kingdee.eas.custom.wages.food;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class TAttendanceRecordsEntryCollection extends AbstractObjectCollection 
{
    public TAttendanceRecordsEntryCollection()
    {
        super(TAttendanceRecordsEntryInfo.class);
    }
    public boolean add(TAttendanceRecordsEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(TAttendanceRecordsEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(TAttendanceRecordsEntryInfo item)
    {
        return removeObject(item);
    }
    public TAttendanceRecordsEntryInfo get(int index)
    {
        return(TAttendanceRecordsEntryInfo)getObject(index);
    }
    public TAttendanceRecordsEntryInfo get(Object key)
    {
        return(TAttendanceRecordsEntryInfo)getObject(key);
    }
    public void set(int index, TAttendanceRecordsEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(TAttendanceRecordsEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(TAttendanceRecordsEntryInfo item)
    {
        return super.indexOf(item);
    }
}