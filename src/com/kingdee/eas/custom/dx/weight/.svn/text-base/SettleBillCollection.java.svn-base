package com.kingdee.eas.custom.dx.weight;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class SettleBillCollection extends AbstractObjectCollection 
{
    public SettleBillCollection()
    {
        super(SettleBillInfo.class);
    }
    public boolean add(SettleBillInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(SettleBillCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(SettleBillInfo item)
    {
        return removeObject(item);
    }
    public SettleBillInfo get(int index)
    {
        return(SettleBillInfo)getObject(index);
    }
    public SettleBillInfo get(Object key)
    {
        return(SettleBillInfo)getObject(key);
    }
    public void set(int index, SettleBillInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(SettleBillInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(SettleBillInfo item)
    {
        return super.indexOf(item);
    }
}