package com.kingdee.eas.custom.dx.baseset;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class FoodDailyCollection extends AbstractObjectCollection 
{
    public FoodDailyCollection()
    {
        super(FoodDailyInfo.class);
    }
    public boolean add(FoodDailyInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(FoodDailyCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(FoodDailyInfo item)
    {
        return removeObject(item);
    }
    public FoodDailyInfo get(int index)
    {
        return(FoodDailyInfo)getObject(index);
    }
    public FoodDailyInfo get(Object key)
    {
        return(FoodDailyInfo)getObject(key);
    }
    public void set(int index, FoodDailyInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(FoodDailyInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(FoodDailyInfo item)
    {
        return super.indexOf(item);
    }
}