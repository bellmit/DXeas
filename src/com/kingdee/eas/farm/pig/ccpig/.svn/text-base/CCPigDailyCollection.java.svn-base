package com.kingdee.eas.farm.pig.ccpig;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class CCPigDailyCollection extends AbstractObjectCollection 
{
    public CCPigDailyCollection()
    {
        super(CCPigDailyInfo.class);
    }
    public boolean add(CCPigDailyInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(CCPigDailyCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(CCPigDailyInfo item)
    {
        return removeObject(item);
    }
    public CCPigDailyInfo get(int index)
    {
        return(CCPigDailyInfo)getObject(index);
    }
    public CCPigDailyInfo get(Object key)
    {
        return(CCPigDailyInfo)getObject(key);
    }
    public void set(int index, CCPigDailyInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(CCPigDailyInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(CCPigDailyInfo item)
    {
        return super.indexOf(item);
    }
}