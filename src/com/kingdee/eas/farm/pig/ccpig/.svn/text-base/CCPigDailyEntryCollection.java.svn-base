package com.kingdee.eas.farm.pig.ccpig;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class CCPigDailyEntryCollection extends AbstractObjectCollection 
{
    public CCPigDailyEntryCollection()
    {
        super(CCPigDailyEntryInfo.class);
    }
    public boolean add(CCPigDailyEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(CCPigDailyEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(CCPigDailyEntryInfo item)
    {
        return removeObject(item);
    }
    public CCPigDailyEntryInfo get(int index)
    {
        return(CCPigDailyEntryInfo)getObject(index);
    }
    public CCPigDailyEntryInfo get(Object key)
    {
        return(CCPigDailyEntryInfo)getObject(key);
    }
    public void set(int index, CCPigDailyEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(CCPigDailyEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(CCPigDailyEntryInfo item)
    {
        return super.indexOf(item);
    }
}