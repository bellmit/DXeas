package com.kingdee.eas.farm.pig.breedingboar;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class BoarDailyEntryFeedDetailEntryCollection extends AbstractObjectCollection 
{
    public BoarDailyEntryFeedDetailEntryCollection()
    {
        super(BoarDailyEntryFeedDetailEntryInfo.class);
    }
    public boolean add(BoarDailyEntryFeedDetailEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(BoarDailyEntryFeedDetailEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(BoarDailyEntryFeedDetailEntryInfo item)
    {
        return removeObject(item);
    }
    public BoarDailyEntryFeedDetailEntryInfo get(int index)
    {
        return(BoarDailyEntryFeedDetailEntryInfo)getObject(index);
    }
    public BoarDailyEntryFeedDetailEntryInfo get(Object key)
    {
        return(BoarDailyEntryFeedDetailEntryInfo)getObject(key);
    }
    public void set(int index, BoarDailyEntryFeedDetailEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(BoarDailyEntryFeedDetailEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(BoarDailyEntryFeedDetailEntryInfo item)
    {
        return super.indexOf(item);
    }
}