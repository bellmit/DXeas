package com.kingdee.eas.farm.pig.breedingboar;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class BoarDailyCullEntryCollection extends AbstractObjectCollection 
{
    public BoarDailyCullEntryCollection()
    {
        super(BoarDailyCullEntryInfo.class);
    }
    public boolean add(BoarDailyCullEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(BoarDailyCullEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(BoarDailyCullEntryInfo item)
    {
        return removeObject(item);
    }
    public BoarDailyCullEntryInfo get(int index)
    {
        return(BoarDailyCullEntryInfo)getObject(index);
    }
    public BoarDailyCullEntryInfo get(Object key)
    {
        return(BoarDailyCullEntryInfo)getObject(key);
    }
    public void set(int index, BoarDailyCullEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(BoarDailyCullEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(BoarDailyCullEntryInfo item)
    {
        return super.indexOf(item);
    }
}