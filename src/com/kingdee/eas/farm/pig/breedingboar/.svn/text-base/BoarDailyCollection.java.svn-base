package com.kingdee.eas.farm.pig.breedingboar;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class BoarDailyCollection extends AbstractObjectCollection 
{
    public BoarDailyCollection()
    {
        super(BoarDailyInfo.class);
    }
    public boolean add(BoarDailyInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(BoarDailyCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(BoarDailyInfo item)
    {
        return removeObject(item);
    }
    public BoarDailyInfo get(int index)
    {
        return(BoarDailyInfo)getObject(index);
    }
    public BoarDailyInfo get(Object key)
    {
        return(BoarDailyInfo)getObject(key);
    }
    public void set(int index, BoarDailyInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(BoarDailyInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(BoarDailyInfo item)
    {
        return super.indexOf(item);
    }
}