package com.kingdee.eas.farm.pig.breedingboar;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class BoarDailyEntryCollection extends AbstractObjectCollection 
{
    public BoarDailyEntryCollection()
    {
        super(BoarDailyEntryInfo.class);
    }
    public boolean add(BoarDailyEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(BoarDailyEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(BoarDailyEntryInfo item)
    {
        return removeObject(item);
    }
    public BoarDailyEntryInfo get(int index)
    {
        return(BoarDailyEntryInfo)getObject(index);
    }
    public BoarDailyEntryInfo get(Object key)
    {
        return(BoarDailyEntryInfo)getObject(key);
    }
    public void set(int index, BoarDailyEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(BoarDailyEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(BoarDailyEntryInfo item)
    {
        return super.indexOf(item);
    }
}