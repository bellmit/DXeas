package com.kingdee.eas.farm.pig.breedingsow;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class ReserveSowEntryCollection extends AbstractObjectCollection 
{
    public ReserveSowEntryCollection()
    {
        super(ReserveSowEntryInfo.class);
    }
    public boolean add(ReserveSowEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(ReserveSowEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(ReserveSowEntryInfo item)
    {
        return removeObject(item);
    }
    public ReserveSowEntryInfo get(int index)
    {
        return(ReserveSowEntryInfo)getObject(index);
    }
    public ReserveSowEntryInfo get(Object key)
    {
        return(ReserveSowEntryInfo)getObject(key);
    }
    public void set(int index, ReserveSowEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(ReserveSowEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(ReserveSowEntryInfo item)
    {
        return super.indexOf(item);
    }
}