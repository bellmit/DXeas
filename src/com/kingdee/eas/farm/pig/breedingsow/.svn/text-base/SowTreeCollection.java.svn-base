package com.kingdee.eas.farm.pig.breedingsow;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class SowTreeCollection extends AbstractObjectCollection 
{
    public SowTreeCollection()
    {
        super(SowTreeInfo.class);
    }
    public boolean add(SowTreeInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(SowTreeCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(SowTreeInfo item)
    {
        return removeObject(item);
    }
    public SowTreeInfo get(int index)
    {
        return(SowTreeInfo)getObject(index);
    }
    public SowTreeInfo get(Object key)
    {
        return(SowTreeInfo)getObject(key);
    }
    public void set(int index, SowTreeInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(SowTreeInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(SowTreeInfo item)
    {
        return super.indexOf(item);
    }
}