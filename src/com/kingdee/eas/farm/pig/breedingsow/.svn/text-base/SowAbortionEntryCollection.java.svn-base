package com.kingdee.eas.farm.pig.breedingsow;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class SowAbortionEntryCollection extends AbstractObjectCollection 
{
    public SowAbortionEntryCollection()
    {
        super(SowAbortionEntryInfo.class);
    }
    public boolean add(SowAbortionEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(SowAbortionEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(SowAbortionEntryInfo item)
    {
        return removeObject(item);
    }
    public SowAbortionEntryInfo get(int index)
    {
        return(SowAbortionEntryInfo)getObject(index);
    }
    public SowAbortionEntryInfo get(Object key)
    {
        return(SowAbortionEntryInfo)getObject(key);
    }
    public void set(int index, SowAbortionEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(SowAbortionEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(SowAbortionEntryInfo item)
    {
        return super.indexOf(item);
    }
}