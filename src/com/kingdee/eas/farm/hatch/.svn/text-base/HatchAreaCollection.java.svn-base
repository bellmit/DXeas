package com.kingdee.eas.farm.hatch;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class HatchAreaCollection extends AbstractObjectCollection 
{
    public HatchAreaCollection()
    {
        super(HatchAreaInfo.class);
    }
    public boolean add(HatchAreaInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(HatchAreaCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(HatchAreaInfo item)
    {
        return removeObject(item);
    }
    public HatchAreaInfo get(int index)
    {
        return(HatchAreaInfo)getObject(index);
    }
    public HatchAreaInfo get(Object key)
    {
        return(HatchAreaInfo)getObject(key);
    }
    public void set(int index, HatchAreaInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(HatchAreaInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(HatchAreaInfo item)
    {
        return super.indexOf(item);
    }
}