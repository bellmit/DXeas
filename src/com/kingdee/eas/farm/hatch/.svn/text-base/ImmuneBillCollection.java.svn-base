package com.kingdee.eas.farm.hatch;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class ImmuneBillCollection extends AbstractObjectCollection 
{
    public ImmuneBillCollection()
    {
        super(ImmuneBillInfo.class);
    }
    public boolean add(ImmuneBillInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(ImmuneBillCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(ImmuneBillInfo item)
    {
        return removeObject(item);
    }
    public ImmuneBillInfo get(int index)
    {
        return(ImmuneBillInfo)getObject(index);
    }
    public ImmuneBillInfo get(Object key)
    {
        return(ImmuneBillInfo)getObject(key);
    }
    public void set(int index, ImmuneBillInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(ImmuneBillInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(ImmuneBillInfo item)
    {
        return super.indexOf(item);
    }
}