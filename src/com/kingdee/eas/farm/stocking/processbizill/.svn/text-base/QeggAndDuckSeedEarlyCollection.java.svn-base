package com.kingdee.eas.farm.stocking.processbizill;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class QeggAndDuckSeedEarlyCollection extends AbstractObjectCollection 
{
    public QeggAndDuckSeedEarlyCollection()
    {
        super(QeggAndDuckSeedEarlyInfo.class);
    }
    public boolean add(QeggAndDuckSeedEarlyInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(QeggAndDuckSeedEarlyCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(QeggAndDuckSeedEarlyInfo item)
    {
        return removeObject(item);
    }
    public QeggAndDuckSeedEarlyInfo get(int index)
    {
        return(QeggAndDuckSeedEarlyInfo)getObject(index);
    }
    public QeggAndDuckSeedEarlyInfo get(Object key)
    {
        return(QeggAndDuckSeedEarlyInfo)getObject(key);
    }
    public void set(int index, QeggAndDuckSeedEarlyInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(QeggAndDuckSeedEarlyInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(QeggAndDuckSeedEarlyInfo item)
    {
        return super.indexOf(item);
    }
}