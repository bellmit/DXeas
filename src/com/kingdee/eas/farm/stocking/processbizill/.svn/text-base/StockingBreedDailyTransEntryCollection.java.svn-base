package com.kingdee.eas.farm.stocking.processbizill;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class StockingBreedDailyTransEntryCollection extends AbstractObjectCollection 
{
    public StockingBreedDailyTransEntryCollection()
    {
        super(StockingBreedDailyTransEntryInfo.class);
    }
    public boolean add(StockingBreedDailyTransEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(StockingBreedDailyTransEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(StockingBreedDailyTransEntryInfo item)
    {
        return removeObject(item);
    }
    public StockingBreedDailyTransEntryInfo get(int index)
    {
        return(StockingBreedDailyTransEntryInfo)getObject(index);
    }
    public StockingBreedDailyTransEntryInfo get(Object key)
    {
        return(StockingBreedDailyTransEntryInfo)getObject(key);
    }
    public void set(int index, StockingBreedDailyTransEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(StockingBreedDailyTransEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(StockingBreedDailyTransEntryInfo item)
    {
        return super.indexOf(item);
    }
}