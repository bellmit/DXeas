package com.kingdee.eas.farm.stocking.processbizill;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class TransFodderBillEntryCollection extends AbstractObjectCollection 
{
    public TransFodderBillEntryCollection()
    {
        super(TransFodderBillEntryInfo.class);
    }
    public boolean add(TransFodderBillEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(TransFodderBillEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(TransFodderBillEntryInfo item)
    {
        return removeObject(item);
    }
    public TransFodderBillEntryInfo get(int index)
    {
        return(TransFodderBillEntryInfo)getObject(index);
    }
    public TransFodderBillEntryInfo get(Object key)
    {
        return(TransFodderBillEntryInfo)getObject(key);
    }
    public void set(int index, TransFodderBillEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(TransFodderBillEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(TransFodderBillEntryInfo item)
    {
        return super.indexOf(item);
    }
}