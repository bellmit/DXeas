package com.kingdee.eas.farm.stocking.processbizill;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class ArrangementBreedBillCollection extends AbstractObjectCollection 
{
    public ArrangementBreedBillCollection()
    {
        super(ArrangementBreedBillInfo.class);
    }
    public boolean add(ArrangementBreedBillInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(ArrangementBreedBillCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(ArrangementBreedBillInfo item)
    {
        return removeObject(item);
    }
    public ArrangementBreedBillInfo get(int index)
    {
        return(ArrangementBreedBillInfo)getObject(index);
    }
    public ArrangementBreedBillInfo get(Object key)
    {
        return(ArrangementBreedBillInfo)getObject(key);
    }
    public void set(int index, ArrangementBreedBillInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(ArrangementBreedBillInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(ArrangementBreedBillInfo item)
    {
        return super.indexOf(item);
    }
}