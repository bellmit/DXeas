package com.kingdee.eas.farm.stocking.processbizill;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class StockingBreedDailyMatUserEntryCollection extends AbstractObjectCollection 
{
    public StockingBreedDailyMatUserEntryCollection()
    {
        super(StockingBreedDailyMatUserEntryInfo.class);
    }
    public boolean add(StockingBreedDailyMatUserEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(StockingBreedDailyMatUserEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(StockingBreedDailyMatUserEntryInfo item)
    {
        return removeObject(item);
    }
    public StockingBreedDailyMatUserEntryInfo get(int index)
    {
        return(StockingBreedDailyMatUserEntryInfo)getObject(index);
    }
    public StockingBreedDailyMatUserEntryInfo get(Object key)
    {
        return(StockingBreedDailyMatUserEntryInfo)getObject(key);
    }
    public void set(int index, StockingBreedDailyMatUserEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(StockingBreedDailyMatUserEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(StockingBreedDailyMatUserEntryInfo item)
    {
        return super.indexOf(item);
    }
}