package com.kingdee.eas.farm.stocking.processbizill;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class ArrangementBreedBillEntryCollection extends AbstractObjectCollection 
{
    public ArrangementBreedBillEntryCollection()
    {
        super(ArrangementBreedBillEntryInfo.class);
    }
    public boolean add(ArrangementBreedBillEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(ArrangementBreedBillEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(ArrangementBreedBillEntryInfo item)
    {
        return removeObject(item);
    }
    public ArrangementBreedBillEntryInfo get(int index)
    {
        return(ArrangementBreedBillEntryInfo)getObject(index);
    }
    public ArrangementBreedBillEntryInfo get(Object key)
    {
        return(ArrangementBreedBillEntryInfo)getObject(key);
    }
    public void set(int index, ArrangementBreedBillEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(ArrangementBreedBillEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(ArrangementBreedBillEntryInfo item)
    {
        return super.indexOf(item);
    }
}