package com.kingdee.eas.farm.stocking.basedata;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class MarginTypeCollection extends AbstractObjectCollection 
{
    public MarginTypeCollection()
    {
        super(MarginTypeInfo.class);
    }
    public boolean add(MarginTypeInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(MarginTypeCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(MarginTypeInfo item)
    {
        return removeObject(item);
    }
    public MarginTypeInfo get(int index)
    {
        return(MarginTypeInfo)getObject(index);
    }
    public MarginTypeInfo get(Object key)
    {
        return(MarginTypeInfo)getObject(key);
    }
    public void set(int index, MarginTypeInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(MarginTypeInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(MarginTypeInfo item)
    {
        return super.indexOf(item);
    }
}