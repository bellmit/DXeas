package com.kingdee.eas.farm.stocking.basedata;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class MarginTypeTreeCollection extends AbstractObjectCollection 
{
    public MarginTypeTreeCollection()
    {
        super(MarginTypeTreeInfo.class);
    }
    public boolean add(MarginTypeTreeInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(MarginTypeTreeCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(MarginTypeTreeInfo item)
    {
        return removeObject(item);
    }
    public MarginTypeTreeInfo get(int index)
    {
        return(MarginTypeTreeInfo)getObject(index);
    }
    public MarginTypeTreeInfo get(Object key)
    {
        return(MarginTypeTreeInfo)getObject(key);
    }
    public void set(int index, MarginTypeTreeInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(MarginTypeTreeInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(MarginTypeTreeInfo item)
    {
        return super.indexOf(item);
    }
}