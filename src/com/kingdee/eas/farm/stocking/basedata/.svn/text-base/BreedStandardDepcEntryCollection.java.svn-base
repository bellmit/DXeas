package com.kingdee.eas.farm.stocking.basedata;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class BreedStandardDepcEntryCollection extends AbstractObjectCollection 
{
    public BreedStandardDepcEntryCollection()
    {
        super(BreedStandardDepcEntryInfo.class);
    }
    public boolean add(BreedStandardDepcEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(BreedStandardDepcEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(BreedStandardDepcEntryInfo item)
    {
        return removeObject(item);
    }
    public BreedStandardDepcEntryInfo get(int index)
    {
        return(BreedStandardDepcEntryInfo)getObject(index);
    }
    public BreedStandardDepcEntryInfo get(Object key)
    {
        return(BreedStandardDepcEntryInfo)getObject(key);
    }
    public void set(int index, BreedStandardDepcEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(BreedStandardDepcEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(BreedStandardDepcEntryInfo item)
    {
        return super.indexOf(item);
    }
}