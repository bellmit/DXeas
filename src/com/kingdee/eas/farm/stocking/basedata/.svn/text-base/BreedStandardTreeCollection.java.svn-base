package com.kingdee.eas.farm.stocking.basedata;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class BreedStandardTreeCollection extends AbstractObjectCollection 
{
    public BreedStandardTreeCollection()
    {
        super(BreedStandardTreeInfo.class);
    }
    public boolean add(BreedStandardTreeInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(BreedStandardTreeCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(BreedStandardTreeInfo item)
    {
        return removeObject(item);
    }
    public BreedStandardTreeInfo get(int index)
    {
        return(BreedStandardTreeInfo)getObject(index);
    }
    public BreedStandardTreeInfo get(Object key)
    {
        return(BreedStandardTreeInfo)getObject(key);
    }
    public void set(int index, BreedStandardTreeInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(BreedStandardTreeInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(BreedStandardTreeInfo item)
    {
        return super.indexOf(item);
    }
}