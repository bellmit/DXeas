package com.kingdee.eas.farm.stocking.basedata;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class FarmersFarmEntryCollection extends AbstractObjectCollection 
{
    public FarmersFarmEntryCollection()
    {
        super(FarmersFarmEntryInfo.class);
    }
    public boolean add(FarmersFarmEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(FarmersFarmEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(FarmersFarmEntryInfo item)
    {
        return removeObject(item);
    }
    public FarmersFarmEntryInfo get(int index)
    {
        return(FarmersFarmEntryInfo)getObject(index);
    }
    public FarmersFarmEntryInfo get(Object key)
    {
        return(FarmersFarmEntryInfo)getObject(key);
    }
    public void set(int index, FarmersFarmEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(FarmersFarmEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(FarmersFarmEntryInfo item)
    {
        return super.indexOf(item);
    }
}