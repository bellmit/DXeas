package com.kingdee.eas.farm.stocking.basedata;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class BreedStandardCollection extends AbstractObjectCollection 
{
    public BreedStandardCollection()
    {
        super(BreedStandardInfo.class);
    }
    public boolean add(BreedStandardInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(BreedStandardCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(BreedStandardInfo item)
    {
        return removeObject(item);
    }
    public BreedStandardInfo get(int index)
    {
        return(BreedStandardInfo)getObject(index);
    }
    public BreedStandardInfo get(Object key)
    {
        return(BreedStandardInfo)getObject(key);
    }
    public void set(int index, BreedStandardInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(BreedStandardInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(BreedStandardInfo item)
    {
        return super.indexOf(item);
    }
}