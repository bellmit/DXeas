package com.kingdee.eas.farm.stocking.assistfunction;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class RunningDailyPaperCollection extends AbstractObjectCollection 
{
    public RunningDailyPaperCollection()
    {
        super(RunningDailyPaperInfo.class);
    }
    public boolean add(RunningDailyPaperInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(RunningDailyPaperCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(RunningDailyPaperInfo item)
    {
        return removeObject(item);
    }
    public RunningDailyPaperInfo get(int index)
    {
        return(RunningDailyPaperInfo)getObject(index);
    }
    public RunningDailyPaperInfo get(Object key)
    {
        return(RunningDailyPaperInfo)getObject(key);
    }
    public void set(int index, RunningDailyPaperInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(RunningDailyPaperInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(RunningDailyPaperInfo item)
    {
        return super.indexOf(item);
    }
}