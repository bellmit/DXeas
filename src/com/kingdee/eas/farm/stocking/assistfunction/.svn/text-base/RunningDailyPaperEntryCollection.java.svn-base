package com.kingdee.eas.farm.stocking.assistfunction;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class RunningDailyPaperEntryCollection extends AbstractObjectCollection 
{
    public RunningDailyPaperEntryCollection()
    {
        super(RunningDailyPaperEntryInfo.class);
    }
    public boolean add(RunningDailyPaperEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(RunningDailyPaperEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(RunningDailyPaperEntryInfo item)
    {
        return removeObject(item);
    }
    public RunningDailyPaperEntryInfo get(int index)
    {
        return(RunningDailyPaperEntryInfo)getObject(index);
    }
    public RunningDailyPaperEntryInfo get(Object key)
    {
        return(RunningDailyPaperEntryInfo)getObject(key);
    }
    public void set(int index, RunningDailyPaperEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(RunningDailyPaperEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(RunningDailyPaperEntryInfo item)
    {
        return super.indexOf(item);
    }
}