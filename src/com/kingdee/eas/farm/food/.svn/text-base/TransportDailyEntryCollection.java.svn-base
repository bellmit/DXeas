package com.kingdee.eas.farm.food;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class TransportDailyEntryCollection extends AbstractObjectCollection 
{
    public TransportDailyEntryCollection()
    {
        super(TransportDailyEntryInfo.class);
    }
    public boolean add(TransportDailyEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(TransportDailyEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(TransportDailyEntryInfo item)
    {
        return removeObject(item);
    }
    public TransportDailyEntryInfo get(int index)
    {
        return(TransportDailyEntryInfo)getObject(index);
    }
    public TransportDailyEntryInfo get(Object key)
    {
        return(TransportDailyEntryInfo)getObject(key);
    }
    public void set(int index, TransportDailyEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(TransportDailyEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(TransportDailyEntryInfo item)
    {
        return super.indexOf(item);
    }
}