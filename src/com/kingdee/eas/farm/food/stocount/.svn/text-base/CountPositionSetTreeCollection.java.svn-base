package com.kingdee.eas.farm.food.stocount;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class CountPositionSetTreeCollection extends AbstractObjectCollection 
{
    public CountPositionSetTreeCollection()
    {
        super(CountPositionSetTreeInfo.class);
    }
    public boolean add(CountPositionSetTreeInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(CountPositionSetTreeCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(CountPositionSetTreeInfo item)
    {
        return removeObject(item);
    }
    public CountPositionSetTreeInfo get(int index)
    {
        return(CountPositionSetTreeInfo)getObject(index);
    }
    public CountPositionSetTreeInfo get(Object key)
    {
        return(CountPositionSetTreeInfo)getObject(key);
    }
    public void set(int index, CountPositionSetTreeInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(CountPositionSetTreeInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(CountPositionSetTreeInfo item)
    {
        return super.indexOf(item);
    }
}