package com.kingdee.eas.farm.food.stocount;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class CountPositionSetMaterialEntryCollection extends AbstractObjectCollection 
{
    public CountPositionSetMaterialEntryCollection()
    {
        super(CountPositionSetMaterialEntryInfo.class);
    }
    public boolean add(CountPositionSetMaterialEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(CountPositionSetMaterialEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(CountPositionSetMaterialEntryInfo item)
    {
        return removeObject(item);
    }
    public CountPositionSetMaterialEntryInfo get(int index)
    {
        return(CountPositionSetMaterialEntryInfo)getObject(index);
    }
    public CountPositionSetMaterialEntryInfo get(Object key)
    {
        return(CountPositionSetMaterialEntryInfo)getObject(key);
    }
    public void set(int index, CountPositionSetMaterialEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(CountPositionSetMaterialEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(CountPositionSetMaterialEntryInfo item)
    {
        return super.indexOf(item);
    }
}