package com.kingdee.eas.farm.feemanager.basedata;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class OilPriceTreeCollection extends AbstractObjectCollection 
{
    public OilPriceTreeCollection()
    {
        super(OilPriceTreeInfo.class);
    }
    public boolean add(OilPriceTreeInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(OilPriceTreeCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(OilPriceTreeInfo item)
    {
        return removeObject(item);
    }
    public OilPriceTreeInfo get(int index)
    {
        return(OilPriceTreeInfo)getObject(index);
    }
    public OilPriceTreeInfo get(Object key)
    {
        return(OilPriceTreeInfo)getObject(key);
    }
    public void set(int index, OilPriceTreeInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(OilPriceTreeInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(OilPriceTreeInfo item)
    {
        return super.indexOf(item);
    }
}