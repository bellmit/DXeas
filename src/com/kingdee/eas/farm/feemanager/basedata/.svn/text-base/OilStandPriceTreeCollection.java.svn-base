package com.kingdee.eas.farm.feemanager.basedata;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class OilStandPriceTreeCollection extends AbstractObjectCollection 
{
    public OilStandPriceTreeCollection()
    {
        super(OilStandPriceTreeInfo.class);
    }
    public boolean add(OilStandPriceTreeInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(OilStandPriceTreeCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(OilStandPriceTreeInfo item)
    {
        return removeObject(item);
    }
    public OilStandPriceTreeInfo get(int index)
    {
        return(OilStandPriceTreeInfo)getObject(index);
    }
    public OilStandPriceTreeInfo get(Object key)
    {
        return(OilStandPriceTreeInfo)getObject(key);
    }
    public void set(int index, OilStandPriceTreeInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(OilStandPriceTreeInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(OilStandPriceTreeInfo item)
    {
        return super.indexOf(item);
    }
}