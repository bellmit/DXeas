package com.kingdee.eas.farm.feemanager.basedata;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class OilStandPriceCollection extends AbstractObjectCollection 
{
    public OilStandPriceCollection()
    {
        super(OilStandPriceInfo.class);
    }
    public boolean add(OilStandPriceInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(OilStandPriceCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(OilStandPriceInfo item)
    {
        return removeObject(item);
    }
    public OilStandPriceInfo get(int index)
    {
        return(OilStandPriceInfo)getObject(index);
    }
    public OilStandPriceInfo get(Object key)
    {
        return(OilStandPriceInfo)getObject(key);
    }
    public void set(int index, OilStandPriceInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(OilStandPriceInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(OilStandPriceInfo item)
    {
        return super.indexOf(item);
    }
}