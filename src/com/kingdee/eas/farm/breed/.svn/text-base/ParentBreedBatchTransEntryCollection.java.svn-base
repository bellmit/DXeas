package com.kingdee.eas.farm.breed;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class ParentBreedBatchTransEntryCollection extends AbstractObjectCollection 
{
    public ParentBreedBatchTransEntryCollection()
    {
        super(ParentBreedBatchTransEntryInfo.class);
    }
    public boolean add(ParentBreedBatchTransEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(ParentBreedBatchTransEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(ParentBreedBatchTransEntryInfo item)
    {
        return removeObject(item);
    }
    public ParentBreedBatchTransEntryInfo get(int index)
    {
        return(ParentBreedBatchTransEntryInfo)getObject(index);
    }
    public ParentBreedBatchTransEntryInfo get(Object key)
    {
        return(ParentBreedBatchTransEntryInfo)getObject(key);
    }
    public void set(int index, ParentBreedBatchTransEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(ParentBreedBatchTransEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(ParentBreedBatchTransEntryInfo item)
    {
        return super.indexOf(item);
    }
}