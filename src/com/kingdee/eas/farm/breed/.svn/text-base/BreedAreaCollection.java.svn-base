package com.kingdee.eas.farm.breed;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class BreedAreaCollection extends AbstractObjectCollection 
{
    public BreedAreaCollection()
    {
        super(BreedAreaInfo.class);
    }
    public boolean add(BreedAreaInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(BreedAreaCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(BreedAreaInfo item)
    {
        return removeObject(item);
    }
    public BreedAreaInfo get(int index)
    {
        return(BreedAreaInfo)getObject(index);
    }
    public BreedAreaInfo get(Object key)
    {
        return(BreedAreaInfo)getObject(key);
    }
    public void set(int index, BreedAreaInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(BreedAreaInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(BreedAreaInfo item)
    {
        return super.indexOf(item);
    }
}