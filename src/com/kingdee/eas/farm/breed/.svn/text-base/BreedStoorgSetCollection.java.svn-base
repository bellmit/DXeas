package com.kingdee.eas.farm.breed;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class BreedStoorgSetCollection extends AbstractObjectCollection 
{
    public BreedStoorgSetCollection()
    {
        super(BreedStoorgSetInfo.class);
    }
    public boolean add(BreedStoorgSetInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(BreedStoorgSetCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(BreedStoorgSetInfo item)
    {
        return removeObject(item);
    }
    public BreedStoorgSetInfo get(int index)
    {
        return(BreedStoorgSetInfo)getObject(index);
    }
    public BreedStoorgSetInfo get(Object key)
    {
        return(BreedStoorgSetInfo)getObject(key);
    }
    public void set(int index, BreedStoorgSetInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(BreedStoorgSetInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(BreedStoorgSetInfo item)
    {
        return super.indexOf(item);
    }
}