package com.kingdee.eas.farm.breed;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class FeedFettleCollection extends AbstractObjectCollection 
{
    public FeedFettleCollection()
    {
        super(FeedFettleInfo.class);
    }
    public boolean add(FeedFettleInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(FeedFettleCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(FeedFettleInfo item)
    {
        return removeObject(item);
    }
    public FeedFettleInfo get(int index)
    {
        return(FeedFettleInfo)getObject(index);
    }
    public FeedFettleInfo get(Object key)
    {
        return(FeedFettleInfo)getObject(key);
    }
    public void set(int index, FeedFettleInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(FeedFettleInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(FeedFettleInfo item)
    {
        return super.indexOf(item);
    }
}