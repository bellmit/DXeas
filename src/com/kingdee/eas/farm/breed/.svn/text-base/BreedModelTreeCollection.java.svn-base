package com.kingdee.eas.farm.breed;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class BreedModelTreeCollection extends AbstractObjectCollection 
{
    public BreedModelTreeCollection()
    {
        super(BreedModelTreeInfo.class);
    }
    public boolean add(BreedModelTreeInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(BreedModelTreeCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(BreedModelTreeInfo item)
    {
        return removeObject(item);
    }
    public BreedModelTreeInfo get(int index)
    {
        return(BreedModelTreeInfo)getObject(index);
    }
    public BreedModelTreeInfo get(Object key)
    {
        return(BreedModelTreeInfo)getObject(key);
    }
    public void set(int index, BreedModelTreeInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(BreedModelTreeInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(BreedModelTreeInfo item)
    {
        return super.indexOf(item);
    }
}