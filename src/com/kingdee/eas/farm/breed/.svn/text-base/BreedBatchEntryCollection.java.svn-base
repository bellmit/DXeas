package com.kingdee.eas.farm.breed;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class BreedBatchEntryCollection extends AbstractObjectCollection 
{
    public BreedBatchEntryCollection()
    {
        super(BreedBatchEntryInfo.class);
    }
    public boolean add(BreedBatchEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(BreedBatchEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(BreedBatchEntryInfo item)
    {
        return removeObject(item);
    }
    public BreedBatchEntryInfo get(int index)
    {
        return(BreedBatchEntryInfo)getObject(index);
    }
    public BreedBatchEntryInfo get(Object key)
    {
        return(BreedBatchEntryInfo)getObject(key);
    }
    public void set(int index, BreedBatchEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(BreedBatchEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(BreedBatchEntryInfo item)
    {
        return super.indexOf(item);
    }
}