package com.kingdee.eas.farm.breed.layegg;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class ParentEggDailyFodderEntryCollection extends AbstractObjectCollection 
{
    public ParentEggDailyFodderEntryCollection()
    {
        super(ParentEggDailyFodderEntryInfo.class);
    }
    public boolean add(ParentEggDailyFodderEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(ParentEggDailyFodderEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(ParentEggDailyFodderEntryInfo item)
    {
        return removeObject(item);
    }
    public ParentEggDailyFodderEntryInfo get(int index)
    {
        return(ParentEggDailyFodderEntryInfo)getObject(index);
    }
    public ParentEggDailyFodderEntryInfo get(Object key)
    {
        return(ParentEggDailyFodderEntryInfo)getObject(key);
    }
    public void set(int index, ParentEggDailyFodderEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(ParentEggDailyFodderEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(ParentEggDailyFodderEntryInfo item)
    {
        return super.indexOf(item);
    }
}