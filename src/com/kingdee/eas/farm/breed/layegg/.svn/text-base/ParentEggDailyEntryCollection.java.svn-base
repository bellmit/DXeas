package com.kingdee.eas.farm.breed.layegg;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class ParentEggDailyEntryCollection extends AbstractObjectCollection 
{
    public ParentEggDailyEntryCollection()
    {
        super(ParentEggDailyEntryInfo.class);
    }
    public boolean add(ParentEggDailyEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(ParentEggDailyEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(ParentEggDailyEntryInfo item)
    {
        return removeObject(item);
    }
    public ParentEggDailyEntryInfo get(int index)
    {
        return(ParentEggDailyEntryInfo)getObject(index);
    }
    public ParentEggDailyEntryInfo get(Object key)
    {
        return(ParentEggDailyEntryInfo)getObject(key);
    }
    public void set(int index, ParentEggDailyEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(ParentEggDailyEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(ParentEggDailyEntryInfo item)
    {
        return super.indexOf(item);
    }
}