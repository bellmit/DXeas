package com.kingdee.eas.farm.breed.layegg;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class LayEggDailyCullDeathEntryCollection extends AbstractObjectCollection 
{
    public LayEggDailyCullDeathEntryCollection()
    {
        super(LayEggDailyCullDeathEntryInfo.class);
    }
    public boolean add(LayEggDailyCullDeathEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(LayEggDailyCullDeathEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(LayEggDailyCullDeathEntryInfo item)
    {
        return removeObject(item);
    }
    public LayEggDailyCullDeathEntryInfo get(int index)
    {
        return(LayEggDailyCullDeathEntryInfo)getObject(index);
    }
    public LayEggDailyCullDeathEntryInfo get(Object key)
    {
        return(LayEggDailyCullDeathEntryInfo)getObject(key);
    }
    public void set(int index, LayEggDailyCullDeathEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(LayEggDailyCullDeathEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(LayEggDailyCullDeathEntryInfo item)
    {
        return super.indexOf(item);
    }
}