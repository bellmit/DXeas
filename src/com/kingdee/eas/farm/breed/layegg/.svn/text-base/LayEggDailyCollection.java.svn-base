package com.kingdee.eas.farm.breed.layegg;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class LayEggDailyCollection extends AbstractObjectCollection 
{
    public LayEggDailyCollection()
    {
        super(LayEggDailyInfo.class);
    }
    public boolean add(LayEggDailyInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(LayEggDailyCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(LayEggDailyInfo item)
    {
        return removeObject(item);
    }
    public LayEggDailyInfo get(int index)
    {
        return(LayEggDailyInfo)getObject(index);
    }
    public LayEggDailyInfo get(Object key)
    {
        return(LayEggDailyInfo)getObject(key);
    }
    public void set(int index, LayEggDailyInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(LayEggDailyInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(LayEggDailyInfo item)
    {
        return super.indexOf(item);
    }
}