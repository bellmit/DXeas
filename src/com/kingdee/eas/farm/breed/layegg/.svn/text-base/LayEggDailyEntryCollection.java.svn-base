package com.kingdee.eas.farm.breed.layegg;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class LayEggDailyEntryCollection extends AbstractObjectCollection 
{
    public LayEggDailyEntryCollection()
    {
        super(LayEggDailyEntryInfo.class);
    }
    public boolean add(LayEggDailyEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(LayEggDailyEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(LayEggDailyEntryInfo item)
    {
        return removeObject(item);
    }
    public LayEggDailyEntryInfo get(int index)
    {
        return(LayEggDailyEntryInfo)getObject(index);
    }
    public LayEggDailyEntryInfo get(Object key)
    {
        return(LayEggDailyEntryInfo)getObject(key);
    }
    public void set(int index, LayEggDailyEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(LayEggDailyEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(LayEggDailyEntryInfo item)
    {
        return super.indexOf(item);
    }
}