package com.kingdee.eas.farm.breed.layegg;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class ParentEggDailyCollection extends AbstractObjectCollection 
{
    public ParentEggDailyCollection()
    {
        super(ParentEggDailyInfo.class);
    }
    public boolean add(ParentEggDailyInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(ParentEggDailyCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(ParentEggDailyInfo item)
    {
        return removeObject(item);
    }
    public ParentEggDailyInfo get(int index)
    {
        return(ParentEggDailyInfo)getObject(index);
    }
    public ParentEggDailyInfo get(Object key)
    {
        return(ParentEggDailyInfo)getObject(key);
    }
    public void set(int index, ParentEggDailyInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(ParentEggDailyInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(ParentEggDailyInfo item)
    {
        return super.indexOf(item);
    }
}