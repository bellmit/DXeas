package com.kingdee.eas.farm.breed.layegg;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class ParentEggDailyTransEntryCollection extends AbstractObjectCollection 
{
    public ParentEggDailyTransEntryCollection()
    {
        super(ParentEggDailyTransEntryInfo.class);
    }
    public boolean add(ParentEggDailyTransEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(ParentEggDailyTransEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(ParentEggDailyTransEntryInfo item)
    {
        return removeObject(item);
    }
    public ParentEggDailyTransEntryInfo get(int index)
    {
        return(ParentEggDailyTransEntryInfo)getObject(index);
    }
    public ParentEggDailyTransEntryInfo get(Object key)
    {
        return(ParentEggDailyTransEntryInfo)getObject(key);
    }
    public void set(int index, ParentEggDailyTransEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(ParentEggDailyTransEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(ParentEggDailyTransEntryInfo item)
    {
        return super.indexOf(item);
    }
}