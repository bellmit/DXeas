package com.kingdee.eas.farm.breed.brood;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class BroodDailyAssEntryCollection extends AbstractObjectCollection 
{
    public BroodDailyAssEntryCollection()
    {
        super(BroodDailyAssEntryInfo.class);
    }
    public boolean add(BroodDailyAssEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(BroodDailyAssEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(BroodDailyAssEntryInfo item)
    {
        return removeObject(item);
    }
    public BroodDailyAssEntryInfo get(int index)
    {
        return(BroodDailyAssEntryInfo)getObject(index);
    }
    public BroodDailyAssEntryInfo get(Object key)
    {
        return(BroodDailyAssEntryInfo)getObject(key);
    }
    public void set(int index, BroodDailyAssEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(BroodDailyAssEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(BroodDailyAssEntryInfo item)
    {
        return super.indexOf(item);
    }
}