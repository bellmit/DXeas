package com.kingdee.eas.farm.breed.brood;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class BroodDailyCollection extends AbstractObjectCollection 
{
    public BroodDailyCollection()
    {
        super(BroodDailyInfo.class);
    }
    public boolean add(BroodDailyInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(BroodDailyCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(BroodDailyInfo item)
    {
        return removeObject(item);
    }
    public BroodDailyInfo get(int index)
    {
        return(BroodDailyInfo)getObject(index);
    }
    public BroodDailyInfo get(Object key)
    {
        return(BroodDailyInfo)getObject(key);
    }
    public void set(int index, BroodDailyInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(BroodDailyInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(BroodDailyInfo item)
    {
        return super.indexOf(item);
    }
}