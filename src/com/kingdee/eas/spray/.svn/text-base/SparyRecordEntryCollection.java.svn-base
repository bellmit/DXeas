package com.kingdee.eas.spray;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class SparyRecordEntryCollection extends AbstractObjectCollection 
{
    public SparyRecordEntryCollection()
    {
        super(SparyRecordEntryInfo.class);
    }
    public boolean add(SparyRecordEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(SparyRecordEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(SparyRecordEntryInfo item)
    {
        return removeObject(item);
    }
    public SparyRecordEntryInfo get(int index)
    {
        return(SparyRecordEntryInfo)getObject(index);
    }
    public SparyRecordEntryInfo get(Object key)
    {
        return(SparyRecordEntryInfo)getObject(key);
    }
    public void set(int index, SparyRecordEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(SparyRecordEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(SparyRecordEntryInfo item)
    {
        return super.indexOf(item);
    }
}