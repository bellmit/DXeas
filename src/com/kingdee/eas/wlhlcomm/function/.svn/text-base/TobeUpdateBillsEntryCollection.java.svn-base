package com.kingdee.eas.wlhlcomm.function;

import com.kingdee.bos.dao.AbstractObjectCollection;
import com.kingdee.bos.dao.IObjectPK;

public class TobeUpdateBillsEntryCollection extends AbstractObjectCollection 
{
    public TobeUpdateBillsEntryCollection()
    {
        super(TobeUpdateBillsEntryInfo.class);
    }
    public boolean add(TobeUpdateBillsEntryInfo item)
    {
        return addObject(item);
    }
    public boolean addCollection(TobeUpdateBillsEntryCollection item)
    {
        return addObjectCollection(item);
    }
    public boolean remove(TobeUpdateBillsEntryInfo item)
    {
        return removeObject(item);
    }
    public TobeUpdateBillsEntryInfo get(int index)
    {
        return(TobeUpdateBillsEntryInfo)getObject(index);
    }
    public TobeUpdateBillsEntryInfo get(Object key)
    {
        return(TobeUpdateBillsEntryInfo)getObject(key);
    }
    public void set(int index, TobeUpdateBillsEntryInfo item)
    {
        setObject(index, item);
    }
    public boolean contains(TobeUpdateBillsEntryInfo item)
    {
        return containsObject(item);
    }
    public boolean contains(Object key)
    {
        return containsKey(key);
    }
    public int indexOf(TobeUpdateBillsEntryInfo item)
    {
        return super.indexOf(item);
    }
}